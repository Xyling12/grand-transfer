# 🚀 Grand Transfer - Статус проекта (Февраль 2026)

Этот файл создан для того, чтобы быстро вспомнить контекст и продолжить работу на другом компьютере.

## 📦 Архитектура и Деплой
Сайт и бот развернуты на VPS (Beget) с помощью **Dokploy**.

**Ключевая особенность:** Приложение разделено на 2 отдельных контейнера (`web` и `bot`), чтобы избежать конфликтов портов и зависаний.

### Конфигурация Dokploy (Вкладка Raw)
Поскольку в Dokploy используется ручной ввод Compose (`Raw`), **изменения в файле `docker-compose.yml` на GitHub не применяются там автоматически!** 
Если нужно пересоздать приложение, в окно Compose в Dokploy нужно вставлять этот код:

```yaml
version: '3.8'
services:
  web:
    build:
      context: https://github.com/Xyling12/grand-transfer.git#main
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - dokploy-network
    volumes:
      - app_data:/app/prisma
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
      - TELEGRAM_BOT_TOKEN=8410817305:AAE6vTlmSuBBWA2Wbwf9_p5wy3QzqF6INz8
      - TELEGRAM_CHAT_ID=376060133
      - EMAIL_PASS=wororvoeoxunilkg
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.site-router.rule=Host(`xn--c1acbe2apap.com`) || Host(`www.xn--c1acbe2apap.com`)"
      - "traefik.http.services.site-router.loadbalancer.server.port=3000"
      - "traefik.docker.network=dokploy-network"

  bot:
    build:
      context: https://github.com/Xyling12/grand-transfer.git#main
      dockerfile: Dockerfile
    command: sh -c "chmod +x wait-for-db.sh && ./wait-for-db.sh"
    restart: unless-stopped
    networks:
      - dokploy-network
    volumes:
      - app_data:/app/prisma
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
      - TELEGRAM_BOT_TOKEN=8410817305:AAE6vTlmSuBBWA2Wbwf9_p5wy3QzqF6INz8
      - TELEGRAM_CHAT_ID=376060133
      - EMAIL_PASS=wororvoeoxunilkg

networks:
  dokploy-network:
    external: true

volumes:
  app_data:
```

---

## 🛠️ Что было исправлено (Последние работы)

### 1. Ошибка Базы Данных в Боте (Table main.Driver does not exist)
**Причина:** Контейнер бота запускался быстрее, чем сайт успевал создать SQLite базу `dev.db`.
**Решение:** 
- База перенесена на общий раздел `volumes: app_data:/app/prisma`.
- Написан Bash-скрипт `wait-for-db.sh`. Бот теперь "спит" и ждет, пока таблица не появится физически, и только потом подключается к Telegram. Ошибка `/start` устранена.

### 2. Запасная отправка писем (Nodemailer)
**Причина:** Пользователь попросил дублировать заявки на почту `romanbatkovic1@yandex.ru`, чтобы ничего не потерялось, если бот ляжет.
**Решение:** 
- Добавлен пакет `nodemailer`.
- Настроен `src/lib/email.ts` на Yandex SMTP.
- Для прохождения авторизации в Яндексе добавлен Пароль Приложения в переменные `EMAIL_PASS=wororvoeoxunilkg`.

### 3. Ошибки сборки Dokploy (npm ci и No Space Left)
**Причина:** Сборка падала из-за рассинхрона `package-lock.json` и нехватки места от старых кэшей.
**Решение:** 
- В `Dockerfile` жесткая команда `npm ci` заменена на `npm install`.
- Сервер очищен скриптами от мусорных образов Docker, освобождено 6 ГБ.

---

## ☀️ Что нужно проверить завтра утром:
1. Открыть `межгород.com` и отправить тестовую заявку.
2. Проверить Telegram (должно прийти уведомление).
3. Проверить почту `romanbatkovic1@yandex.ru` (должно прийти письмо с таблицей).
4. Написать боту `/start` (он должен ответить без ошибки БД).

Если всё работает — можно привязывать SSL и финальные домены!
