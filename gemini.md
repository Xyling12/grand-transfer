# 🚀 Grand Transfer - Статус проекта (Февраль 2026)

Этот файл создан для того, чтобы быстро вспомнить контекст и продолжить работу на другом компьютере.
**Важное примечание:** Основной домен проекта — `межгород.com` (Punycode: `xn--c1acbe2apap.com`). Не путать с названием репозитория/проекта `grand-transfer`.

## 📦 Архитектура и Деплой (v1.3.2)
Сайт и бот развернуты на мощном облачном сервере **Timeweb Cloud** с помощью **Dokploy**.

**Ключевая особенность:** Из-за высоких требований Next.js к оперативной памяти при сборке (build), мы перенесли тяжелый процесс компиляции на бесплатные сервера Microsoft Github (CI/CD Pipeline). Сервер Timeweb теперь занимается только *запуском* уже готового легковесного Docker-образа.

Приложение по-прежнему разделено на 2 отдельных контейнера (`web` и `bot`), чтобы избежать конфликтов портов и зависаний.

### 🔄 Как теперь заливать правки на сайт и в бота
Теперь процесс деплоя стал невероятно простым и отказоустойчивым:

1. **Пишем код и сохраняем:** Вы, как и раньше, просите ИИ внести изменения в код на вашем компьютере.
2. **Отправляем на GitHub (Push):** Вводите в терминале стандартные команды Git:
   ```bash
   git add .
   git commit -m "Описание ваших правок"
   git push
   ```
3. **Ждем сборку на GitHub:** Как только код попадает в ветку `main`, GitHub автоматичеки запускает `Actions` (за кулисами компилирует Next.js). Это занимает около 3-4 минут. (Прогресс можно посмотреть во вкладке "Actions" репозитория на сайте Github).
4. **Обновляем сервер в Dokploy:**
   - Заходите в Dokploy: `http://185.171.82.112:3000`
   - Открываете проект `grand-transfer`.
   - Нажимаете кнопку **Deploy**.
   Dokploy за пару секунд скачает свежий готовый образ (`ghcr.io/...`) и мгновенно перезапустит сайт и бота без малейшей нагрузки на сервер!

### Конфигурация Dokploy (Вкладка Raw)
Поскольку мы перешли на готовый образ Github Container Registry, наш `docker-compose.yml` теперь выглядит так (его нужно вставлять в Dokploy, если проект пересоздается с нуля):

```yaml
version: '3.8'
services:
  web:
    image: ghcr.io/xyling12/grand-transfer:main
    pull_policy: always
    restart: unless-stopped
    networks:
      - dokploy-network
    volumes:
      - app_data:/app/prisma
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
      - TELEGRAM_BOT_TOKEN=8410817305:AAE6vTlmSuBBWA2Wbwf9_p5wy3QzqF6INz8
      - TELEGRAM_CHAT_ID=376060133
      - EMAIL_PASS=wororvoeoxunilkg
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.site-router.rule=Host(`xn--c1acbe2apap.com`) || Host(`www.xn--c1acbe2apap.com`)"
      - "traefik.http.services.site-router.loadbalancer.server.port=3000"
      - "traefik.docker.network=dokploy-network"

  bot:
    image: ghcr.io/xyling12/grand-transfer:main
    pull_policy: always
    command: sh -c "chmod +x wait-for-db.sh && ./wait-for-db.sh"
    restart: unless-stopped
    networks:
      - dokploy-network
    volumes:
      - app_data:/app/prisma
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
      - TELEGRAM_BOT_TOKEN=8410817305:AAE6vTlmSuBBWA2Wbwf9_p5wy3QzqF6INz8
      - TELEGRAM_CHAT_ID=376060133
      - EMAIL_PASS=wororvoeoxunilkg

networks:
  dokploy-network:
    external: true

volumes:
  app_data:
```

---

## 🛠️ Последние исправления и обновления

### 1. Премиум-дизайн "Liquid Glass"
- **Визуал:** Внедрена темная тема, золотые акценты и эффекты стекломорфизма.
- **Типографика:** `Bodoni Moda` (заголовки) и `Jost` (основной текст).
- **Секции:** Обновлены Hero, Тарифы, Форма бронирования, Отзывы и Футер.
- **UI/UX фиксы:** Исправлено поведение выпадающего списка автокомплита (теперь закрывается по клику вне элемента), исправлены цвета UI и поведение обновления логотипа.

### 2. Работа с картами (Текущий статус)
- **Переход:** Сайт временно переводился на **2GIS** для точности до подъезда.
- **Решение:** Из-за ограничений бесплатного периода 2GIS, принято решение вернуться к **Yandex Maps (v2.1)** для долгосрочной стабильности.

### 3. Расширенная CRM и Telegram Bot (v1.3.8)
- **Приватность и SEO:** Добавлена страница Политики Конфиденциальности (`/privacy`), баннер согласия на использование Cookie и чекбокс в форме бронирования (для соответствия ФЗ-152). Добавлен SVG-favicon для удовлетворения требований Яндекс.Вебмастера. Исправлен URL sitemap в `robots.ts`.
- **Bot v1.3.8 (Многошаговая Регистрация и Модерация):** Полноценная машина состояний (State Machine) для сбора данных Водителей. Бот запрашивает Контактный номер, ФИО, ПТС, Фото Водительских прав и Фото Автомобиля с сохранением `file_id` в БД Prisma. Добавлены команды модерации: `/approve`, `/reject`, `/ban`, `/unban` и `/add_driver` (экспресс-регистрация). В меню админа появилась кнопка "💻 CRM Система".
- **Web CRM Админ Панель:** Разработаны дашборды `/admin/drivers`, `/admin/clients`, `/admin/orders`.
  - 🚨 **Безопасность панели (ФЗ-152):** HTTP Basic Auth заменён на интегрированный **Telegram Login Widget**. Авторизация аппаратно защищена на уровне сервера (Next.js Middleware) через **JWT (jose) cookies**. Вход разрешен только пользователям, прошедшим сверку Telegram ID с ролью `ADMIN` в БД. Убедитесь, что `JWT_SECRET` (или `TELEGRAM_BOT_TOKEN`) проставлен на сервере.
  - 🖼️ **Медиа-Шлюз Telegram:** Реализован API-эндпоинт `/api/tg-file`, который безопасно проксирует оригиналы фото (ПТС, СТС, Авто) с серверов Telegram в браузер администратора и в ссылки выгрузки Excel.
  - 📊 **Доска Заказов:** Сводная таблица всех заказов с привязкой Диспетчеров и Водителей и статусами поездок.
- **Excel Экспорт по Месяцам:** Алгоритм `xlsx` в боте переписан. Теперь выгрузка БД содержит: заказы, базу водителей (с рабочими прямыми ссылками на фото), таблицу уникальных клиентов и их выручки.
- **Модерация закрытого чата:** Бот выдает одноразовые ссылки-приглашения (на 24 часа) через кнопку "💬 Чат". Бот автоматически удаляет из группы: мат, ссылки, политику и системные сообщения о входе/выходе.

### 4. Инфраструктура, Оптимизация деплоя и Общая БД
- **Смерть от Свопа (OOM)**: Сборка Next.js перенесена на GitHub Actions (GHCR). Timeweb (2GB RAM) теперь только "запускает" готовый легкий образ.
- **Очистка БД в Dokploy**: `schema.prisma` теперь принудительно копируется из бэкапа в `/app/prisma/schema.prisma` при старте через `wait-for-db.sh`, решая проблему игнорирования обновленной схемы.
- **RAM Next.js**: Бэкенд переведен в режим `output: "standalone"`, потребление памяти снижено с 500+ МБ до ~80 МБ.
- 🔗 **Общая База Данных (Main + Light):** Оба домена (`xn--c1acbe2apap.com` и `taximezhgorod777.ru`) используют **одну общую базу данных**. В Dokploy это реализовано так: проект `grand-transfer` создает Docker Volume (например, `grandtransfer-xxxx_app_data`). А проект `grand-transfer-light` в своем `docker-compose.yml` подключает этот же Volume как `external: true`. Таким образом, оба контейнера читают/пишут в один физический файл `dev.db`. (Схемы Prisma синхронизированы в обоих репозиториях).

---

## ☀️ Что нужно сделать / проверить дальше:
1. Завершить восстановление функционала **Yandex Maps** в форме бронирования.
2. Проверить работу отправки писем на почту (после исправления SMTP таймаутов).
3. Убедиться, что политика конфиденциальности и баннер куки отображаются корректно на мобильных устройствах.
4. Протестировать обновленный функционал бота (кнопка "Заказ завершен" и переводы статусов).

---
*Последнее обновление: 26.02.2026*
