# 🚀 Grand Transfer - Статус проекта (Февраль 2026)

Этот файл создан для того, чтобы быстро вспомнить контекст и продолжить работу на другом компьютере.

## 📦 Архитектура и Деплой
Сайт и бот развернуты на VPS (Beget) с помощью **Dokploy**.

**Ключевая особенность:** Приложение разделено на 2 отдельных контейнера (`web` и `bot`), чтобы избежать конфликтов портов и зависаний.

### Конфигурация Dokploy (Вкладка Raw)
Поскольку в Dokploy используется ручной ввод Compose (`Raw`), **изменения в файле `docker-compose.yml` на GitHub не применяются там автоматически!** 
Если нужно пересоздать приложение, в окно Compose в Dokploy нужно вставлять этот код:

```yaml
version: '3.8'
services:
  web:
    build:
      context: https://github.com/Xyling12/grand-transfer.git#main
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - dokploy-network
    volumes:
      - app_data:/app/prisma
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
      - TELEGRAM_BOT_TOKEN=8410817305:AAE6vTlmSuBBWA2Wbwf9_p5wy3QzqF6INz8
      - TELEGRAM_CHAT_ID=376060133
      - EMAIL_PASS=wororvoeoxunilkg
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.site-router.rule=Host(`xn--c1acbe2apap.com`) || Host(`www.xn--c1acbe2apap.com`)"
      - "traefik.http.services.site-router.loadbalancer.server.port=3000"
      - "traefik.docker.network=dokploy-network"

  bot:
    build:
      context: https://github.com/Xyling12/grand-transfer.git#main
      dockerfile: Dockerfile
    command: sh -c "chmod +x wait-for-db.sh && ./wait-for-db.sh"
    restart: unless-stopped
    networks:
      - dokploy-network
    volumes:
      - app_data:/app/prisma
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
      - TELEGRAM_BOT_TOKEN=8410817305:AAE6vTlmSuBBWA2Wbwf9_p5wy3QzqF6INz8
      - TELEGRAM_CHAT_ID=376060133
      - EMAIL_PASS=wororvoeoxunilkg

networks:
  dokploy-network:
    external: true

volumes:
  app_data:
```

---

## 🛠️ Что было исправлено (Последние работы)

### 1. Роли и Заказы (Диспетчеры)
**Суть:** Внедрена строгая 3-уровневая система ролей (Главный Администратор, Диспетчер, Водитель).
**Реализация:**
- Новые заказы приходят только Админам и Диспетчерам.
- Диспетчер нажимает "Отправить водителям", после чего заказ улетает в общую ленту Водителей со скрытым телефоном и именем клиента.
- Водитель забирает заказ, получая полный доступ к данным, а у остальных заказ удаляется из интерфейса.
- Добавлена возможность регистрироваться, одобрять заявки (с выбором `Диспетчер`/`Водитель`), понижать и полностью удалять (`Выгнать`) пользователей из базы.

### 2. Модерация закрытого чата и Защита Данных
**Суть:** Защита от утечек и спама в чате водителей.
**Реализация:**
- Бот выдает индивидуальные ссылки-приглашения (на 1 вход, на 24 часа) через кнопку "💬 Чат". ID закрытой группы вшит напрямую в код.
- Бот прослушивает группу и автоматически удаляет: сообщения с матом, ссылками, политическими обсуждениями, а также системные уведомления (кто-то зашел/вышел/закрепил сообщение).
- Включена функция `protect_content: true`, запрещающая скриншоты и пересылку личных сообщений с заказами. 

### 3. Ошибка Базы Данных в Dokploy (The column main.Order.dispatcherId does not exist)
**Причина:** При деплое монтируется внешний `volume` на папку `/app/prisma`. Из-за этого Dokploy всегда использовал старый `schema.prisma` файл, лежащий в томе, игнорируя новые поля из GitHub. `npx prisma db push` проходил вхолостую.
**Решение:**
- В `Dockerfile` добавлено создание резервной копии схемы: `COPY --from=builder /app/prisma ./prisma_backup`.
- При старте контейнеров (`Dockerfile` и `wait-for-db.sh`) бот теперь принудительно копирует новый `schema.prisma` с заменой `cp -f /app/prisma_backup/schema.prisma /app/prisma/schema.prisma`, а затем локально на сервере делает пуш в `dev.db`.

### 4. Ошибки сборки Dokploy (npm ci и No Space Left)
**Причина:** Сборка падала из-за рассинхрона `package-lock.json` и нехватки места от старых кэшей.
**Решение:** 
- В `Dockerfile` жесткая команду `npm ci` заменена на `npm install`.
- Сервер очищен скриптами от мусорных образов Docker, освобождено 6 ГБ.

---

## ☀️ Что нужно сделать / проверить:
1. Запросить у бота "👀 Активные заявки" — ошибка базы данных должна исчезнуть.
2. Протестировать отправку заказов тестовым Водителям.
3. Администраторы (в том числе назначенные Главными) должны проверить свои границы прав.

Если всё работает — можно привязывать SSL и финальные домены!
