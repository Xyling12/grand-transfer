# Журнал изменений проекта (GrandTransfer)

Этот файл вести для того, чтобы отслеживать все изменения, внесенные в проект с помощью ИИ, и легко синхронизировать работу на разных устройствах.

## Последние крупные обновления (Февраль 2026)

### 1. Премиальный дизайн (Hero & Typography)
*   **Типографика:** Перешли на шрифтовую пару `Cormorant` (заголовки) и `Montserrat` (текст).
*   **Hero (Главный экран):** Заголовки разделены на 3 строки для лучшего управления стилями и создания воздушности. Адаптированы под мобильные устройства (уменьшение размера шрифта на экранах <768px и <480px).
*   **Кнопки:** Стиль кнопок приведен к единому виду (темный фон, статусная форма pill-shape, золотая обводка и эффект свечения `box-shadow` при наведении).

### 2. Шапка (Header)
*   **Респонсивность (Адаптация):** Доработаны медиа-запросы.
    *   `< 1150px`: Скрывается текст логотипа (остается иконка), чтобы сэкономить место.
    *   `< 900px`: Меню навигации заменяется на "бургер".
    *   `< 768px`: Кнопка телефона (круглая иконка) оставлена видимой, при нехватке места скрываются иконки соцсетей.
*   **Кнопка вызова:** Превращена в аккуратную круглую кнопку, по стилю (размерам и паддингам) совпадающую с кнопками социальных сетей.

### 3. Исправление геолокации и выбора города (`CityContext.tsx`)
*   **Отвязка от вечного кэша:** Логика хранения выбранного города изменена с `localStorage` на `sessionStorage`. Теперь при старте новой сессии с другого места сайт выполнит поиск по IP через `geojs` и обновит город.
*   **Сортировка городов:** В выпадающем списке текущий выбранный город всегда отображается _первым_ (на нулевом индексе), а остальные следуют под ним.

### 4. Очистка кода и багфиксы
*   **Popular Routes:** Убрана приписка расчета "Эконом • 29 ₽/км" (теперь показывается только цена "от 4000 ₽").
*   **Опечатки:** Исправлен регистр "ДНр" на "ДНР" во всех списках.
*   **Изображения:** Стандартный `next/image` (`<Image>`) заменен на обычный HTML-тег `<img>` во многих карточках (например, в тарифах и маршрутах), в связи с проблемами таймаутов на внешних доменах.
*   **SEO:** В `robots.txt` добавлены базовые правила индексации.
*   **Архитектура:** Создана директория `/scripts/`, куда перемещены все служебные Node.js скрипты (вроде автозаполнения координат городов), добавлены скрипты `preview.bat` и `check_site.bat` для быстрого запуска и проверок.

### 5. Интеграция Telegram-бота и Базы Данных (Prisma + SQLite)
*   **База данных:** Подключена ORM `Prisma` на базе легковесной базы `SQLite` (`dev.db`), которая теперь сохраняет все заявки, цены, имена и телефоны локально на сервере.
*   **Уведомления:** Создан скрипт `scripts/bot.ts` и `telegram.ts`. При отправке формы `BookingForm` данные летят на новый API-роут `/api/order/route.ts`, который тут же пересылает сообщение владельцу в Telegram.
*   **Статистика бота:** Внедрена команда `/stats` для подсчета выполненных заказов и выручки, а также защищенная команда `/reset` для обнуления базы.

### 6. Улучшение формы заказа (BookingForm)
*   **UX/UI:** Заменен ползунок пассажиров на удобные кликабельные кнопки (1-8). Добавлено поле для текстовых комментариев к заказу.
*   **Автоопределение города:** Внедрен хук `useGeolocationCity`, который при открытии сайта ищет IP клиента через `ip-api.com` и автоматически выставляет город в секции "Откуда" (по умолчанию Ижевск при неудаче).
*   **Состояния отправки:** Во время запроса к кнопке добавляется лоадер-крутилка. При успехе форма плавно заменяется на красивый блок с галочкой и кнопкой по центру (Сделать новый заказ).

### 7. Юридические документы и Ссылки
*   **GDPR:** Создана новая системная страница `/privacy` с "Политикой конфиденциальности". Ссылка на неё выведена в подвал для соблюдения ФЗ-152.
*   **Соцсети:** Заменены ссылки-заглушки в подвале на актуальные профили пользователя (Telegram, WhatsApp, VK).

### 8. Отзывы (Reviews)
*   **Карусель:** Отключен жесткий `scroll-snap` в CSS. Внедрен умный скроллинг по расчету ширины карточки в пикселях. Добавлены кнопки "Влево-Вправо" для листания отзывов.

### 9. Оптимизация ассетов и Авторство
*   **Изображения:** Все картинки автомобилей и логотипы конвертированы из `png/jpg` в современный `webp` для сверхбыстрой загрузки.
*   **Удаление неиспользуемого:** Полностью удалена папка `public/images/cities` и свойство `heroImage` из массива городов (`src/data/cities.ts`), так как от фотографий городов решено отказаться ради чистоты премиального дизайна.
*   **Favicon:** Заменена стандартная иконка Next.js на кастомный золотой SVG-логотип вкладок.
*   **Footer:** В подвал сайта добавлено указание авторства разработки ("Ившин М.С") с активной ссылкой на Telegram автора.

### 10. Деплой на Vercel и SEO-оптимизация
*   **Vercel Build:** Устранены ошибки кэширования статики `Next.js` при работе с `Prisma` на серверах Vercel. Добавлен скрипт `postinstall: prisma generate` для корректной сборки клиента локальной БД в облаке, а API-эндпоинт заказа переведен в режим `force-dynamic`.
*   **Ошибки TypeScript:** Очищены конфликты версий (удален `prisma.config.ts` и отключен `eslint` в `next.config.ts`), типизирован `telegram.ts` для прохождения строгих линтеров.
*   **Телеграм Маршруты:** В уведомления бота добавлена единая кликабельная ссылка на **комбинированный маршрут** в Яндекс Картах (через параметр `~rtext`), чтобы водитель мог открыть навигатор в один клик.
*   **Индексация SEO:** Добавлены автоматические файлы `/sitemap.xml` и `/robots.txt` для корректной индексации сайта в поисковиках (Google, Yandex). Устранена проблема с кодировкой UTF-16 от Windows PowerShell, файлы пересозданы в `UTF-8`.
*   **Шапка (Header):** На логотип добавлено событие `onClick` для плавного возврата (`smooth scroll`) в самое начало страницы, если пользователь уже находится на Главной.
### 11. Фиксы UI, Геолокации и Тарифов
*   **Отзывы (Reviews):** Заголовок и подзаголовок секции выровнены строго по центру экрана.
*   **Геолокация:** Доработка логики `CityContext.tsx`. Полностью убрано сохранение города в `sessionStorage`. Теперь при каждом новом открытии сайта (заходе с устройства) система всегда приоритетно получает реальную геолокацию пользователя (исправлен баг "залипания" вручную выбранного города).
*   **Калькулятор (Подача):** Убрана базовая фиксированная стоимость "Подачи машины" в 500 рублей изо всех маршрутов (доставка по-прежнему считается от 1500 рублей).
*   **Новый Тариф:** Добавлен новый тариф "Комфорт", который занимает место между "Стандарт" и "Комфорт+". Он добавлен в базу городов, в калькулятор и в карточки тарифов на главной странице.
*   **Доставка (Логика Цены):** Тариф "Доставка" зафиксирован на начальной стоимости "от 1500 ₽" (без учета километража), добавлен предупреждающий блок для пользователя о необходимости уточнить цену у диспетчера.
*   **Карта Маршрутов:** Исправлен баг в `LeafletMapPreview.tsx`, из-за которого при смене конечного или начального пункта оставалась визуальная линия старого маршрута. Линия теперь принудительно очищается (`setRouteGeometry([])`) до построения нового пути.

### 12. Pre-launch SEO & Optimization Audit (antigravity-awesome-skills)
*   **Изображения (Тарифы):** Сгенерированы новые 3D-модели автомобилей для тарифов "Эконом", "Стандарт" и "Минивэн" на строгом `hex #000000` фоне для идеального соответствия темному премиум-стилю сайта. Настроено ручное CSS-масштабирование (`transform: scale`) для каждого автомобиля индивидуально, чтобы они визуально смотрелись одинакового размера в сетке тарифов.
*   **OpenGraph & Twitter (SEO):** В `src/app/layout.tsx` добавлены глобальные метаданные (SEO-теги `keywords`, `openGraph` и `twitter:card`) для формирования красивых ссылок-превью (с изображениями и заголовком) при отправке сайта в мессенджерах (Telegram, WhatsApp) и соцсетях. Задана базовая директива `metadataBase`.
*   **Core Web Vitals (Оптимизация производительности):** Добавлен атрибут `loading="lazy"` ко всем изображениям "ниже сгиба" (в блоках: популярные маршруты, почему мы, отзывы). Это значительно улучшило показатели Largest Contentful Paint (LCP) и предотвращает скачки макета (CLS) при начальной загрузке сайта.

### 13. Инфраструктура, Домен и Аналитика
*   **Домен и Хостинг:** Выполнен подбор и проверка доменов на спам/историю. Зарегистрирован основной домен `межгород.com`. Выбран и оплачен быстрый VPS-сервер на базе Ubuntu 24.04 (Beget: 1 Core, 1 GB RAM, 10 GB NVMe).
*   **Деплой (VPS):** Создан скрипт автоматизированного развертывания проекта. Сервер настроен с использованием связки Nginx (проксирование порта 3000), PM2 (демонизация процесса Next.js) и Certbot (для последующей выписки SSL сертификата Let's Encrypt).
*   **Яндекс Метрика (SPA Analytics):** Внедрен кастомный компонент `YandexMetrika.tsx`, обернутый в `<Suspense>` для исправления ошибки `useSearchParams()` в Next.js 14. Компонент отслеживает классические переходы (`hit`) при рендеринге на стороне клиента (SPA) и инициализирует основной тег.
*   **Отслеживание Целей (Конверсии):** В компоненте `BookingForm.tsx` (при успешном ответе от сервера: `res.ok`) добавлена отправка JavaScript-события `order_submitted` напрямую в счетчик Яндекс Метрики (`106952983`), что позволяет 100% точно отслеживать воронку продаж и стоимость лида.
*   **Маркетинговая База:** Создана структура рекламной кампании `ads_strategy.md` (Горячие/Теплые ключи, обширная минусовка нецелевых запросов, структура текстов объявлений с учетом УТП сайта - фикс цена, квитанции, детские кресла).

### 14. Настройка DNS-маршрутизации (межгород.com)
*   **Проблема с DNS:** Домен был ошибочно привязан в панели хостинга Beget, хотя покупался у регистратора REG.RU. Из-за этого настройки DNS не применялись для глобальной сети интернета.
*   **Решение:** Осуществлен переход в панель оригинального регистратора (REG.RU). Прописаны `A-запись (@)` на IP `216.198.79.1` и `CNAME-запись (www)` на `cname.vercel-dns.com.` для корректной маршрутизации трафика на сервера платформы развертывания Vercel.

### 15. Telegram-бот: Обход блокировок WebView и Умный Маршрут (Smart Route Bridge)
*   **Проблема Deep-link:** При клике на прямые `yandexnavi://` или `yandexmaps://` ссылки из Telegram-бота (Android/iOS), встроенный внутренний браузер мессенджера (WebView) блокировал вызов сторонних приложений из соображений политики безопасности.
*   **Решение (Буферная страница):** Создана техническая Next.js страница-мост `src/app/route/page.tsx` (`/route`). 
*   **Алгоритм работы:**
    * Бот теперь присылает единую красивую веб-ссылку "В Навигаторе / Картах 🚗", ведущую на нашу буферную страницу (вместе с GPS-координатами маршрута).
    * Telegram успешно открывает её как обычный безопасный сайт.
    * Скрипт на странице-мосту детектирует операционную систему. На Android он мгновенно использует системный механизм **`intent://`**, который аппаратно, в обход всех запретов браузера, принудительно командует системе запустить "Яндекс Навигатор" (с резервным вызовом "Яндекс Карт" через секунду).
    * В скрипте также отрисован fallback-интерфейс (3 понятные кнопки: Навигатор, Карты, Браузер) на случай, если автоматический редирект будет заблокирован настройками приватности устройства (например, на iOS). Отправлен push на Vercel.

### 16. Оптимизация SEO и Логика фикс-тарифов
*   **Превью ссылок (OG/Twitter):** Сокращены заголовки `layout.tsx` для корректного отображения без обрезания текста (троеточия) в мессенджерах (Telegram, WhatsApp) — теперь заголовок выглядит как "Такси межгород | GrandTransfer".
*   **Поисковики:** Добавлены обновленные мета-теги верификации (куски кода) для подтверждения прав на домен в Яндекс.Вебмастер и Google Search Console (через HTML-тег).
*   **Сноски (Asterisks):** Ко всем фиксированным тарифам (Доставка, Трезвый водитель) на главном экране и внутри калькулятора добавлена звездочка (*) к ценнику, указывающая на то, что это базовая цена.
*   **Трезвый водитель (Прайсинг):** Тариф переведен на ту же визуальную логику калькулятора, что и "Доставка". Теперь вместо расчета по километражу он показывает фиксированную стоимость ("от 1500 ₽*") и выводит под итоговой суммой предупреждающую плашку: "* Для уточнения окончательной стоимости услуги обратитесь к диспетчеру".

### 17. UI Списки, Z-index и Очистка Данных (Города)
*   **Автокомплит (LeafletSuggestInput):** Кардинально улучшен UX поиска городов. Подсказки теперь появляются уже после ввода 1-й буквы (ранее было от 3-х). Добавлена плавная вращающаяся анимация (CSS `keyframes spin`) к значку загрузки.
*   **Перекрытие слоев (Z-Index):** Исправлены серьезные баги наложения элементов:
    *   Удалено свойство `overflow: hidden` у родительского контейнера карты внутри `BookingForm`, которое "срезало" выпадающий список.
    *   Глобально перестроены веса: Хедеру (`Header`) назначен `z-index: 99999`, контейнеру полей ввода (`.grid`) — `1000`, а картам Leaflet (`MapContainer`) принудительно установлен `z-index: 0`. Это гарантированно избавило списки (выбор городов и тарифы) от проваливания "под" карту.
    *   Фон списка подсказок переведен на системные CSS-переменные `var(--color-secondary)`, решив проблему "прозрачного" нечитаемого текста.
*   **Закрытие по клику (Outside Click):** К бургер-меню (Mobile Menu) и верхнему селектору города (`Header.tsx`) жестко привязан React `ref`, что позволило настроить моментальное закрытие всех менюшек при клике в любую пустую область сайта.
*   **Очистка базы (ДНР/ЛНР):** Написан Node.js скрипт, регулярным выражением (`regex`) очистивший файлы `src/data/cities.ts` и `tariffs.ts` от всех упоминаний (аббревиатур) "ДНР" и "ЛНР". Теперь спорные города (Донецк, Мелитополь, Макеевка и т.д.) отображаются только по имени.

---
_Добавляйте записи в этот файл при выполнении новых крупных задач, чтобы не терять контекст!_
# Telegram Driver Access System Implementation

## Goal
Close the Telegram Bot and/or Driver features so that only approved drivers can receive notifications or interact with the system. Lay the foundation for a future paid-subscription group model. 

## Proposed Architecture

This system shifts the focus from website-auth to Telegram Bot auth.

### 1. Database Schema Updates
We will add a new `Driver` model to Prisma to track authorized Telegram users.

#### `prisma/schema.prisma`
```prisma
model Driver {
  id             String    @id @default(cuid())
  telegramId     BigInt    @unique
  username       String?
  firstName      String?
  status         String    @default("PENDING") // PENDING, APPROVED, BANNED
  // Future subscription fields
  subExpiresAt   DateTime? 
  createdAt      DateTime  @default(now())
}
```

### 2. Telegram Bot Command Handling
In the current setup, `telegram.ts` simply pushes notifications. We need the bot to actually receive messages (like `/start`) from drivers to register their Telegram ID.
We'll implement a simple Next.js API route (`/api/webhook/telegram`) to handle Telegram Webhooks.

#### Logic flow:
1. **Driver:** Sends `/start` to the bot.
2. **Bot Webhook:** Checks if `telegramId` is in the database.
   - If missing: Creates a new `Driver` record with `status: "PENDING"`. Sends "Заявка на доступ отправлена администратору."
   - If `status: "PENDING"`: Sends "Ваша заявка ожидает подтверждения."
   - If `status: "APPROVED"`: Sends "Доступ разрешен. Вы в системе."

### 3. Administrator Control Panel
We need a secure place for the administrator to approve or reject drivers.

#### `/src/app/admin/drivers/page.tsx`
A protected Next.js page (secured by a simple hardcoded admin password or NextAuth) displaying a table of all drivers.
- **Actions:** Buttons for "Одобрить" (Approve) and "Заблокировать" (Ban).
- When a driver is approved, the system can optionally send them a Telegram message via the bot: *"Доступ в закрытый клуб водителей открыт!"*

### 4. Group Integration (Future)
Since the goal is a paid group:
1. The Bot is added as an Administrator to a private Telegram Channel/Group.
2. When a driver's subscription expires, the bot automatically removes them via the Telegram `banChatMember` API.
3. The Bot can generate one-time invite links for newly approved drivers using `createChatInviteLink`.

## User Review Required
1. Do you want the bot to **automatically** invite approved drivers to a private Telegram Group right now, or should we just focus on building the "Одобрение" (Approval) system first?
2. How do you want to secure the Admin panel on the website? A simple secret PIN code, or a full login/password system just for you?
