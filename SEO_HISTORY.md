# История SEO Оптимизации (SEO_HISTORY)

В этом документе зафиксированы все технические и контентные улучшения, направленные на поисковую оптимизацию (SEO) проекта **Grand Transfer**. 

Формат аналогичен Changelog, чтобы наглядно видеть развитие видимости сайта в поисковых системах.

---

## 🚀 Базовая Инфраструктура (Next.js App Router)

В основу архитектуры заложен фреймворк **Next.js 14+ (React 19)**, серверный рендеринг (SSR) которого по умолчанию дает отличный буст для SEO, отдавая поисковым роботам уже готовый HTML-код страницы.

### 🧩 1. Метатеги и Open Graph (Metadata API)
В корневом файле `layout.tsx` реализовано управление глобальными метатегами по стандартам Next.js:
- **Title**: `Междугороднее такси | GrandTransfer`
- **Description**: Расширенное, продающее описание услуг межгорода и поездок на границу (КПП).
- **Keywords**: Набор релевантных ключей (междугороднее такси, такси межгород, минивэн, трезвый водитель).
- **OpenGraph & Twitter Cards**: Разметка для красивого отображения карточки сайта в мессенджерах (Telegram, WhatsApp) и соцсетях. Содержит пути к превью-изображениям (`/images/og-image.jpg`).
- **Канонические ссылки**: В настроенном `metadataBase` (`https://www.xn--c1adbj4b9a7c.com`) зафиксирован основной домен для избежания дублей в поисковиках.

### 🌐 2. Верификация Вебмастеров
Поисковые роботы напрямую подключены к сайту без лишних HTML-файлов в корне. Токены верификации вшиты напрямую в `<head>`:
- **Google Search Console**: `6jdi0lGUwUYQl...`
- **Yandex Webmaster**: `4a87d70a322542ad`

### 📊 3. Аналитика и Разметка
- **Yandex Metrika**: Обернут в отдельный компонент `YandexMetrika.tsx` и вызывается глобально. Настроен сбор конверсий (цель `order_submitted` триггерится при успешной отправке формы в `BookingForm.tsx`).
- **Schema.org (Микроразметка)**: Глобальный компонент `SchemaOrg.tsx` формирует JSON-LD разметку для поисковиков. Это помогает Яндексу и Google однозначно понимать, что это сайт такси/транспортной компании, и выводить расширенные сниппеты в поисковой выдаче.

---

## ⚡ Оптимизация Производительности (Core Web Vitals)
Поисковики (особенно Google) пессимизируют медленные сайты. В Grand Transfer внедрены следующие практики:

### 🖼️ Работа с изображениями
- Все тяжелые графические ресурсы (`economy-3d.webp`, `comfort-3d.webp`) переведены в современный формат **WebP**, который весит в разы меньше стандартных PNG/JPEG, не теряя в качестве прозрачности.

### 💻 Динамические Импорты (Lazy Loading)
- **Карты (Yandex/Leaflet)**: Интерактивный виджет карты — самый тяжелый скрипт на странице. В файле `page.tsx` вся форма бронирования (`BookingForm`) подгружается через `next/dynamic` с флагом `ssr: false`. 
  - **Результат**: Сервер отдает текстовый контент сайта за миллисекунды, а тяжелая логика карт загружается только в браузере клиента *после* отрисовки основного визуала. 

### 🔤 Оптимизация Шрифтов
- Используется модуль `next/font/google` для загрузки премиальных шрифтов (`Bodoni Moda`, `Jost`). 
- **Преимущество**: Шрифты скачиваются сервером на этапе сборки сайта (build) и отдаются вместе с HTML. Это предотвращает прыжки верстки во время загрузки (CLS метрика) и ускоряет First Contentful Paint.

---

## 📝 Семантика и Контент ("Liquid Glass")
При внедрении премиум-дизайна была проработана семантическая верстка:
- Использование правильных HTML5-тегов: `<main>`, `<section>`, `<header>`, `<footer>`. 
- Грамотная иерархия заголовков: На странице только один главный `<h1>` (в блоке Hero), остальные логически разбиты на `<h2>` (Тарифы, Почему мы) и `<h3>` (Карточки маршрутов).
- Адаптивный UX (Mobile-friendly): Кнопки вызова, гамбургер-меню и форма заказа оптимизированы для нажатий пальцем, что прямо влияет на поведенческие факторы (положительный сигнал для SEO).

---

## 🗺️ Генерация Sitemap (XML)
- В `package.json` настроен скрипт пре-сборки: `tsx scripts/generate-sitemap-index.ts && next build`.
- Перед каждым деплоем сайт автоматически компилирует свежую `sitemap.xml` карту для поисковиков, гарантируя быструю индексацию новых маршрутов или страниц (если они будут добавлены).

---
*Документ автоматически поддерживается и обновляется параллельно с новыми SEO-релизами.*
